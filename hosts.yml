all:
  children:
    testservers:
      hosts:
        node1:
          ansible_host: localhost
          ansible_port: 2221
          ansible_user: root
          ansible_password: "{{ vault_root_password }}"
        node2:
          ansible_host: localhost
          ansible_port: 2222
          ansible_user: root
          ansible_password: "{{ vault_root_password }}"
      vars:
        ansible_user: root
