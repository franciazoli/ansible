all:
  children:
    testservers:
      hosts:
        node1:
          ansible_host: localhost
          ansible_port: 2221
        node2:
          ansible_host: localhost
          ansible_port: 2222
      vars:
        ansible_user: ansible
        ansible_ssh_private_key_file: ~/.ssh/ansible_id
